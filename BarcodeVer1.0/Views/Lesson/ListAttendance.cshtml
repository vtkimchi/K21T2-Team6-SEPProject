@model IEnumerable<BarcodeVer1._0.Models.Attendance>

@{
    ViewBag.Title = "ListAttendance";
    Layout = "~/Views/Shared/_Layout1.cshtml";
}

<h2>ListAttendance</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Count_Lesson)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lesson.MaKH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Member.MaSV)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Count_Lesson)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lesson.MaKH)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Member.MaSV)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
